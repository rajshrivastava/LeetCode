class Solution:
    def subtractProductAndSum(self, n: int) -> int:
        n = str(n)
        summation = 0
        prod = 1
        for ch in n:
            summation += int(ch)
            prod *= int(ch)
        return prod - summation
