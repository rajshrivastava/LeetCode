class Solution:
    def reverseWords(self, s: str) -> str:
        words = s.split(' ')
        result = ''
        for word in reversed(words):
            if word:
                result += word + ' '
        return result[:-1]
